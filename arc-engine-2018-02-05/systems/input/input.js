'use strict';

class Input
{
  constructor ( ) // { }
  { }

  init ( p )
  { return new Promise( ( resolve, reject ) => {
    resolve( );
  } ); }
}

module.exports = ( ) => { return new Input( ); }
