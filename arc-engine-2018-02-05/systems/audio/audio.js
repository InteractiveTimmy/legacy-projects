'use strict';

class Audio
{
  constructor ( ) // { }
  { }

  init ( p )
  { return new Promise( ( resolve, reject ) => {
    resolve( );
  } ); }
}

module.exports = ( ) => { return new Audio( ); }
