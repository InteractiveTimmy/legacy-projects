'use strict';

class Physics
{
  constructor ( ) // { }
  { }

  init ( p )
  { return new Promise( ( resolve, reject ) => {
    resolve( );
  } ); }
}

module.exports = ( ) => { return new Physics( ); }
