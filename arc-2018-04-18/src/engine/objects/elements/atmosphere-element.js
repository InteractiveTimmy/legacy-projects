'use strict';

class AtmosphereElement
{
  constructor ( p ) // { entity:@ae.Entity, config:{ } }
  {
    this.entity = p.entity;

    // this.entity.geometry.computeBoundingBox( );
  }

  update ( )
  {
    // this.boxHelper.update( );
    // this.boxHelper.geometry.computeBoundingBox( );
  }
}

module.exports = AtmosphereElement;
